# Syncs a folder of .html or other files with a note generated by a
# singleton Note subclass as documented in <declarative-notes.md>.
# Files may have .yaml metadata as documented in <filesystem-notes.md>.

mappings:
  - dest:
      py: "pkg_test.mod_test.SingletonTest"
    src:
      - md: "assets/md/test1"
      - md: "assets/md/test2"
