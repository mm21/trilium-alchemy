# Installation

It's recommended to create an isolated environment via [uv](https://docs.astral.sh/uv/) so as to more closely track the TriliumAlchemy version installed. This will enable you to upgrade TriliumAlchemy and make any required API changes at your convenience.

## PyPI

Install from PyPI:

```bash
pip install trilium-alchemy
```
